"""
Extended model training for SDP (LightGBM and CatBoost).
Currently just calls the main training which already includes these models.
"""

def main():
    """Train extended models (LightGBM and CatBoost are already included in train_models.py)"""
    print("Extended models (LightGBM, CatBoost) are already trained by train_models.py")
    print("No additional training needed.")